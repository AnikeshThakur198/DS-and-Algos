	An anagram of a string is another string that contains same characters, only the order of characters can be different. For example:
	
		str1: "abcd",
		str2: "dbac"

both strings str1 and str2 are Anagram of each other. 